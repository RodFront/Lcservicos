import React from "react"
import { Container } from "./styles"

const Sections = ({title}) => {
    return(
        <Container>
                <h1>{title}</h1>
        </Container>
    )
}


export default Sections